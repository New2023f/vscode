pool:
  vmImage: "ubuntu-latest"

trigger:
  branches:
    include: ["main", "release/*"]
pr: none

steps:
  - task: NodeTool@0
    inputs:
      versionSpec: "16.x"

  - template: ./download-distro.yml
    parameters:
      VSCODE_QUALITY: ${{ parameters.VSCODE_QUALITY }}
